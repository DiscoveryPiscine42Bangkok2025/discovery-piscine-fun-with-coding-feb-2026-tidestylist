# เช็คว่าถ้าไม่มีการใส่ค่าอะไรมาเลย ($# คือจำนวนค่าที่ส่งมา)
if [ $# -eq 0 ]; then
    echo "No arguments supplied"
else
    # วนลูปสร้างโฟลเดอร์ตามจำนวนค่าที่ส่งมา
    for arg in "$@"
    do
        # สั่งสร้างโฟลเดอร์ โดยเติม ex ไว้ข้างหน้า
        mkdir "ex$arg"
    done
fi